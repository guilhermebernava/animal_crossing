import 'package:flutter/material.dart';

class FossilDetailsView extends StatelessWidget {
  const FossilDetailsView({super.key});

  static const route = "/fossil_detail";

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text(
        "",
      ),
    );
  }
}
